{
  "pre-commit": "pnpm vsh code-workspace --auto-commit && pnpm exec lint-staged --allow-empty --concurrent false",
  "commit-msg": "pnpm exec commitlint --edit $1",
  "post-merge": "pnpm install"
}
